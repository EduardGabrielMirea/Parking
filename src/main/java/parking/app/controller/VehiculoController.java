package parking.app.controller;

import parking.app.entity.Vehiculo;

public class VehiculoController {

/*
    private Vehiculo buscarVehiculo(String matricula) {
        for (Vehiculo v : vehiculosOficiales) {
            if (v.getMatricula().equals(matricula)) {
                return v;
            }
        }
        for (Vehiculo v : vehiculosResidentes) {
            if (v.getMatricula().equals(matricula)) {
                return v;
            }
        }
        return null;
    }

    public void darDeAltaVehiculoOficial(String matricula) {
        vehiculosOficiales.add(new VehiculoOficial(matricula));
        System.out.println("Vehículo oficial con matrícula " + matricula + " dado de alta.");
    }

    public void darDeAltaVehiculoResidente(String matricula) {
        VehiculoResidente vehiculo = new VehiculoResidente(matricula);
        vehiculosResidentes.add(vehiculo);
        pagosResidentes.put(vehiculo, 0);
        System.out.println("Vehículo residente con matrícula " + matricula + " dado de alta.");
    }

 */
}
